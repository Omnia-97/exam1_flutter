import 'package:flutter/material.dart';

class ChartBarTab extends StatelessWidget {
  const ChartBarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Column(
        children: [
          Text('Chart Bar'),
        ],
      ),
    );
  }
}
