import 'package:flutter/material.dart';

class NavPointerTab extends StatelessWidget {
  const NavPointerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Column(
        children: [
          Text('NavPointertab'),
        ],
      ),
    );
  }
}
